import React from 'react'

import { ExpenseHistory } from '../../expense/expenseHistory'

export const ExpenseDetails = () => {
    return (
        <div>
            <ExpenseHistory />
        </div>
    )
}
